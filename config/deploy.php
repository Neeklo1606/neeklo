<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Deploy server URL (for artisan deploy command)
    |--------------------------------------------------------------------------
    | URL сервера, на который отправляется POST /api/deploy после пуша.
    | Должен быть указан в **локальном** .env (где вы запускаете php artisan deploy).
    */
    'server_url' => env('DEPLOY_SERVER_URL'),

    /*
    |--------------------------------------------------------------------------
    | Deploy token
    |--------------------------------------------------------------------------
    | Токен для авторизации запроса деплоя. Тот же токен должен быть в .env на сервере (DEPLOY_TOKEN).
    | Укажите в **локальном** .env.
    */
    'token' => env('DEPLOY_TOKEN'),
];
